class Student 
{
   private:
        int studentId;
        char name[20];
   public:
       void assignDetails(int stuId, char Sname[]);
       int dispalyStudentId();
       int displayName();
        
  
};
